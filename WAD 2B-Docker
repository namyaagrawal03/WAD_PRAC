sudo apt update
sudo apt install docker.io
docker ps
docker images

docker pull openjdk
docker run --name JAVA -it -d openjdk
docker exec -it JAVA jshell

docker stop <container name>
docker container rm <id from above command>
